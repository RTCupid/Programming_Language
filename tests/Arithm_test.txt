my_main ()
{
    x = 10;
    y = 20;
    z = 3;
    sum = x + 1;

    sub = y - 1;

    write_about {sum};

    write_about {sub};

    mult = x * z;

    write_about {mult};

    divi = sum divide z;

    write_about {divi};
};

then_commit_seppuku
