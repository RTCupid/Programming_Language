my_main ()
{
    x = 10;
    y = 20;
    z = 3;

    sum = x + 1;
    sub = y - 1;
    mult = x * z;
    divi = sum divide z;

    write_about {sum};
    write_about {sub};
    write_about {mult};
    write_about {divi};
};

then_commit_seppuku
