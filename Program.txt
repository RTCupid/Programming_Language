input (a);
input (b);
input (c);

if_happens (a multiply b multiply c)
{
    D = b multiply b subtract 4 multiply a multiply c;
    write_about {D};

    if (D < 0)
    {
        write_about {0};
        then_commit_seppuku
    }

    first_x = ((0 subtract 1) multiply b subtract sqrt (D)) divide (2 multiply a);
    write_about {2};
    write_about {first_x};
}

then_commit_seppuku
