my_main ()
{
    input (first_argument);

    write_about {first_argument};

    Function (first_argument);

    then_commit_seppuku
};

Function (arg)
{
    arg = arg + 2;

    write_about {arg};

    return 0;
};

then_commit_seppuku
