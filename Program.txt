input (a);
input (b);
input (c);

if_happens (a * b * c)
{
    D = b * b - 4 * a * c;
    write_about {D};

    if (D < 0)
    {
        write_about {0};
        then_commit_seppuku
    }

    first_x = ((0 - 1) * b - sqrt (D)) / (2 * a);
    write_about {2};
    write_about {first_x};
}


then_commit_seppuku
